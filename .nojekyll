Without this file github.io ignores files and folders that start with _.
Also after every commit it takes a while to update the page with Jekyll because it has to do server side rendering.
Disabling it allows for instant updates after every commit.
